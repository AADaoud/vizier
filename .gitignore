# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store
.claude
.venv
# Vizier runtime data — written by the plugin at runtime, never source
vault_index.json
vizier_runs.jsonl
vizier_runs.jsonl.1
vizier_debug.log
vizier_debug.log.1
memories.json
contradiction_state.json
intake_state.json
scheduler_state.json
